# Vector Store

This folder will contain the local ChromaDB persistence files.

## Purpose

The vector store is used for semantic document retrieval.

Instead of matching exact keywords, it stores embeddings for document chunks and retrieves the most relevant pieces of text for a user query.

